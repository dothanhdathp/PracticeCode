// 31. Next Permutation
#include <algorithm>
#include "stdio.h"
#include <iostream>
#include <vector>

using namespace std;

void nextPermutation(vector<int>& nums) {
    // CASE1: if size = 1 => return;
    // CASE2: if size = 1 => return;
}

int main() {
    vector<int> example = {1, 2, 3};
    nextPermutation(example);
    for(int i:example) std::cout << i << ' ';
    std::cout<<std::endl;
    return 0;
}
1, 1, 8, 9, 1, 1, 1, 1, 1, 1