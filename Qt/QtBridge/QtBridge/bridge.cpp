#include <QObject>
#include "bridge.h"

Bridge::Bridge(QObject *parent)
{
    do { (void)(parent); } while (0);
    // Constructer
    /* Nothing for construct, to be define next :)) */
}

Bridge::~Bridge()
{
    // DeConstructer
}
