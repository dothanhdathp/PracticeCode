#include <string>
#include <vector>
#include <list>
#include <map>
#include <set>

class Trie {
	Trie();
	~Trie();
	
/* value */
public:
	list
private:

/* function */
public:
	void insert();
	void removeItems();
	void showItems();
	void getIimeByText(string text); // should be return list
	
private:
	// NA
}