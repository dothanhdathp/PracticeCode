#include <cstdlib>
#include <iostream>
#include <algorithm>
//#include "stdio.h"

using namespace std; 
int threeSumClosest(vector<int>& nums, int target) {
    int fo = 0;
    int lo = nums.size() - 1;
    int p = fo + lo;
    while(p != target, p)
    cout << endl;
}
int main()
{
	vector<int> test = {-1,2,1,-4};
	int n = threeSumClosest(test);
	cout << n << endl;
	return EXIT_SUCCESS; 
}
